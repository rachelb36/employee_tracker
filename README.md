# EMPLOYEE TRACKER



- Run `psql -U postgres` to open postgres

- Run `\i db/schema.sql` to initiate db schema

- Run `\i db.seeds.sql` to seed database

- Run `\q` to quit postgress

- Install npm `npm install`
